# iOS Support

Placeholder for future iOS-specific performance measurement scripts and configurations.

This directory will contain:
- `build-configs.props` — iOS build configuration presets
- `collect_nettrace.sh` — iOS .nettrace collection
- `print_app_sizes.sh` — iOS app bundle size reporting
- Additional iOS-specific tooling
